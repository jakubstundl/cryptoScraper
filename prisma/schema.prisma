generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model coinCodexPrediction {
  name   String  @id @unique
  time   Float?
  bought Float?
  d      String? @db.Text
  v      String? @db.Text
  p      String? @db.Text
}
